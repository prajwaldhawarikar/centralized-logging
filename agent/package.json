{
  "name": "logging-agent",
  "version": "1.0.0",
  "description": "Logging agent in node.js which transports log to log-service",
  "main": "index.js",
  "scripts": {
    "start": "npm run copy-protos && node src/index.js",
    "copy-protos": "npx cpy ../protos .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grpc/grpc-js": "^1.10.6",
    "@grpc/proto-loader": "^0.7.12"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0"
  }
}
